# ============================================================
# 达人对接系统 - Python 依赖
# ============================================================

# --- 核心依赖 ---
openai>=1.0.0           # LLM API 调用
pyyaml>=6.0             # 配置文件解析
python-dotenv>=1.0.0    # 环境变量加载
openpyxl>=3.1.0         # Excel 导出
pandas>=2.0.0           # 数据处理

# --- MediaCrawler 依赖（按需安装）---
# 进入 MediaCrawler 目录后执行: pip install -r requirements.txt
